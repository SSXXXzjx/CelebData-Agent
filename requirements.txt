# =====================================================================
# Celebrity 完整依赖（核心 Agent + 流水线 + 爬虫 Spider_XHS + 可选 VLM）
# 安装：pip install -r requirements.txt
# =====================================================================

# ----- 核心 Agent -----
httpx>=0.27
PyYAML>=6.0
rich>=13.0
python-dotenv>=1.0
prompt_toolkit>=3.0

# ----- 流水线 / 图片处理 / 相似去重 / YuNet 判断 -----
Pillow>=10.0
numpy>=1.24
imagehash>=4.3
opencv-python>=4.8

# ----- 爬虫 Spider_XHS（来自 third_party/Spider_XHS/requirements.txt）-----
requests>=2.31
loguru>=0.7
retry>=0.9
PyExecJS
curl_cffi==0.15.0
openpyxl>=3.1
aiohttp>=3.9
qrcode>=7.4

# ----- 可选：本地 Qwen3-VL 视觉模型（体积很大，不需要可整段注释）-----
torch>=2.1
transformers>=4.57
accelerate>=0.30
qwen-vl-utils>=0.0.10
modelscope>=1.9
